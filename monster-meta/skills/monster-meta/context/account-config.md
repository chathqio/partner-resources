# Account Config — NOT CONFIGURED

This skill has no account configured yet.

**Run `/monster-meta setup`** — it validates your Meta token, discovers your ad accounts,
pages, and pixels, and writes your real config into this file. Takes about a minute.

Until then, every build/audit step will ask for an account ID and other details by hand,
and no UTM scheme or audit-log path is set.

> Prerequisite: the `meta-graph` MCP must be connected with your own Meta access token
> (`META_ACCESS_TOKEN`). See `docs/meta-token-setup.md` for how to create one.
