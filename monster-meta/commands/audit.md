---
description: Monster Meta — pull performance data and diagnose campaign issues
argument-hint: "[account/campaign + timeframe]"
---

Invoke the **monster-meta** skill and run its `audit` workflow (`tasks/audit-campaign.md`) to pull
live performance data via the meta-graph MCP and produce a framework-backed diagnosis. Context: $ARGUMENTS
