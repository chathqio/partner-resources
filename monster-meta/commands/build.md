---
description: Monster Meta — deploy a campaign/ad set/ad/creative to Meta from a plan (PAUSED by default)
argument-hint: "[plan or campaign to build]"
---

Invoke the **monster-meta** skill and run its `build` workflow (`tasks/build-campaign.md`) to deploy
a campaign/ad set/ad/creative to Meta via the meta-graph MCP. Create objects PAUSED by default and
never auto-activate without explicit confirmation. Context: $ARGUMENTS
